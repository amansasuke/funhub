<form method="post" action="{{ path('app_doc_for_client_delete', {'id': doc_for_client.id,'ord': app.request.get('orderid')}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ doc_for_client.id) }}">
    <button class="btn">Delete</button>
</form>
