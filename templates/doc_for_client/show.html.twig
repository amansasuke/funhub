{% extends 'dashstyle.html.twig' %}

{% block title %}DocForClient{% endblock %}

{% block body %}
    <h1>DocForClient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ doc_for_client.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ doc_for_client.Name }}</td>
            </tr>
            <tr>
                <th>Doclink</th>
                <td>{{ doc_for_client.Doclink }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ doc_for_client.Status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_doc_for_client_index') }}">back to list</a>

    <a href="{{ path('app_doc_for_client_edit', {'id': doc_for_client.id}) }}">edit</a>

    {{ include('doc_for_client/_delete_form.html.twig') }}
{% endblock %}
